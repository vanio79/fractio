# Fractio Web Dashboard - Routes Index
#
# This module exports all routes for easy importing.

import dashboard, nodes, metrics, storage, clock, data, sql, settings

export dashboard
export nodes
export metrics
export storage
export clock
export data
export sql
export settings
